# C++23 Logger Examples - Bazel Build Configuration

load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "basic_usage",
    srcs = ["basic_usage.cpp"],
    copts = [
        "-std=c++23",
        "-stdlib=libc++",
    ],
    linkopts = [
        "-lc++",
        "-lpthread",
    ],
    deps = [
        "//:logger",
    ],
)

cc_binary(
    name = "named_templates",
    srcs = ["named_templates.cpp"],
    copts = [
        "-std=c++23",
        "-stdlib=libc++",
    ],
    linkopts = [
        "-lc++",
        "-lpthread",
    ],
    deps = [
        "//:logger",
    ],
)
